# Frontend build-time configuration.
# Copy to frontend/.env.local for local development.

# Shows the development account picker on the login page (usernames only,
# never passwords). MUST be false or unset for any shared or production build.
VITE_ENABLE_DEV_LOGIN=true
